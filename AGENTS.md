# AGENTS.md — قواعد العمل (إلزامية لأي وكيل/مطور)

## الهوية
«سوق اللغة» — أكاديمية إندونيسية/تركية أوفلاين للمتحدثين بالعربية. **المنصة: أندرويد Native (Jetpack Compose) في `android/` — هو المنتج الوحيد الشاجَع.** ملفات الويب في الجذر (`index.html`, `js/`, `css/`, `sw.js`) استوديو تأليف ومعاينة للمحتوى فقط، ولا تُنسَخ إلى الـ APK إطلاقًا.

## القواعد الصارمة
1. **أوفلاين أولًا**: لا اعتماد شبكة في أي وظيفة تعلم أساسية.
2. **لا أزرار وهمية** ولا Lorem Ipsum ولا بيانات حشو.
3. **صدق الوظائف**: كل فجوة تُوثَّق في docs/IMPLEMENTATION_STATUS.md.
4. **الترجمة الطبيعية** لا الحرفية — الحرفي للتعليم فقط.
5. **لا اختراع فروق** لغوية/جنسية غير موجودة؛ وثّق ما لا يتغير.
6. **الثقافة حذرة** سياقية («تختلف حسب المنطقة»).
7. **فصل المحتوى عن التقدم**: تقدم المستخدم في localStorage بمعرفات ثابتة — تحديث المحتوى لا يمسّه.
8. **معرفات ثابتة فريدة** لكل عنصر (بادئة-رقم).
9. **بنية عبارة موحدة**: `{a,i,it,t,tt,lv:1-4,p:1-5,w,n,lit?,alt?}`.
10. **قبل الرفع**: `node validate.mjs` و`node test/engine.test.mjs` و`node tools/gen_android_content.mjs --check` تمرّ جميعًا. تغيير عبارات في `data/**` ⇒ شغّل المولّد وارفع `FullContent.kt` معه. وعند تغيير ملفات المعاينة فقط حدّث `CACHE` في sw.js.
11. **خصوصية**: لا مفاتيح/أسرار/رفع بيانات/تتبع.
12. **RTL عربي** والنص الهدف LTR.
13. **إضافة لغة** = ملفات بيانات بنفس المخطط + حقول العرض/TTS؛ لا تفترض أن `i`/`t` يكفيان بلا مراجعة الواجهة.
14. **مجاني بالكامل**: لا اشتراك ولا إعلانات ولا مشتريات داخل التطبيق.
15. **منصة واحدة Native**: أندرويد Compose هو المنتج — لا WebView ولا نسخ لملفات الويب في الأصول/البناء. مصدر حقيقة العبارات: `data/**` ← `tools/gen_android_content.mjs` ← `FullContent.kt`. القاموس والحوارات والقواعد والقصص والأفعال والتحليلات والمدربون نُقلت كاملةً إلى Kotlin وهي تُحرَّر هناك مباشرة؛ لتوسعتها آليًا وسّع المولّد (انظر ROADMAP R8).

## التشغيل
البناء: افتح `android/` في Android Studio أو CI في `.github/workflows/android.yml` (يخرج APK في الإصدارات). المعاينة السريعة للمحتوى: افتح `index.html` أو `python3 -m http.server 8080`.
فحص البيانات: `node validate.mjs` — اختبار المحرك: `node test/engine.test.mjs` — مزامنة/فحص محتوى أندرويد: `node tools/gen_android_content.mjs [--check]`.
مصدر العبارات: `data/**` — منطق المعاينة: `js/**` — تطبيق أندرويد: `android/**` (منطق المجال في `domain/`) — التوثيق: `docs/**`.
العبارات المستوردة أحادية اللغة (`data/core/imported_learn.js`) خارج البنك الافتراضي ولا تدخل التطبيق.
